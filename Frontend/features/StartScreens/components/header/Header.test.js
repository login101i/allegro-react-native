import { render, screen, fireEvent, cleanup } from "@testing-library/dom";
import Header from './Header'

let getByTestId;

describe("headerTest", () => {
  test(" There is a container in header", ()=>{
     
  });
});
